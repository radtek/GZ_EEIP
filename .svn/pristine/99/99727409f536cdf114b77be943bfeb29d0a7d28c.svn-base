package org.pangolin.eeip.webservice;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class AnalysisWebService extends BaseWebService {
	
	private static final Logger log = LoggerFactory.getLogger(AnalysisWebService.class);
	
	/**
	 * TODO
	 * 保存刑事案件立案数
	 * @param year 年度
	 * @param month 月度
	 * @param orgSn 机构代码
	 * @param qty 刑事案件立案数
	 * @return 保存状态（0：失败；1：成功）
	 * */
	public String saveCaseQty(int year, int month, String orgSn, int qty) {
		try {
			
		} catch(Exception ex) {
			log.error(ex.getMessage());
		}
		return "";
	}
}