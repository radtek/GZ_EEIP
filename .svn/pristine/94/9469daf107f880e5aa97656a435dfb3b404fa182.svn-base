package org.pangolin.eeip.webservice;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;


public class PreappraisalWebService extends BaseWebService {
	
	private static final Logger log = LoggerFactory.getLogger(PreappraisalWebService.class);
	
	/**
	 * TODO
	 * 登记物证预检信息
	 * @author Oscar Mok
	 * @param orgSn 机构代码
	 * @param caseSn 案件编号
	 * @param caseName 案件名称
	 * @param entrusters 委托人（格式：[{ "alias" : "test1", "policeId" : "123456" },{ "alias" : "test2", "policeId" : "123456" }]）
	 * @param evids 待鉴定检材清单（格式：[{ "name" : "evid1", "desc" : "desc1" },{ "name" : "evid2", "desc" : "desc2" }]）
	 * @return 登记状态（0：失败；1：成功）
	 * */
	public String savePreappraisal(String orgSn, String caseSn, String caseName, String entrusters, String evids) {
		try {
			
		} catch(Exception ex) {
			log.error(ex.getMessage());
		}
		return "";
	}
	
}